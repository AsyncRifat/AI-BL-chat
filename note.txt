<!DOCTYPE html>
<html lang="bn">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BanglaLink AI Chat Ad</title>
  <link rel="stylesheet" href="style.css">
</head>

<body>

  <div class="chat-widget">
    <div class="chat-container">

      <div class="logo"
        style="display: flex; justify-content: center; align-items: center;  border-bottom: 1px solid rgb(227, 220, 220);">
        <img src="./logo.png" alt="logo">
      </div>

      <div class="chat-box" id="chatBox">
        <div class="bot-msg">
          <span style="font-size: 32px;">🤖</span> <span style="font-size: 12px;">আমি বাংলালিংক AI বন্ধু বলছি,</span>
          <br> <span>আমি আপনাকে কিভাবে
            সাহায্য করতে পারি?</span>
        </div>
      </div>

      <div class="input-area">
        <input type="text" id="userInput" placeholder="যেমন লিখুন: মিনিট / ইন্টারনেট / এসএমএস ...">
        <button id="sendBtn">পাঠান</button>
      </div>
    </div>
  </div>

  <script src="script.js"></script>
</body>

</html>


body {
  margin: 0;
  padding: 0;
  background: #f2f2f2;
  font-family: 'Noto Sans Bengali', sans-serif;
}

/* Fixed position bottom-right widget */
.chat-widget {
  position: fixed;
  right: 20px;
  bottom: 20px;
  z-index: 9999;
}

/* Chat container */
.chat-container {
  width: 300px;
  height: 600px;
  border: 0.5px solid rgb(255, 165, 0);
  border-radius: 5px;
  background: #fff;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  overflow: hidden;
  box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.15);
}

/* logo */
.logo img {
  width: 155px;
  height: 30px;
  margin: 9px 0 9px 0;
}

/* Chat area */
.chat-box {
  flex: 1;
  overflow-y: auto;
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

/* Bot and user messages */
.bot-msg,
.user-msg {
  max-width: 80%;
  padding: 8px 10px;
  border-radius: 5px;
  font-size: 15px;
  line-height: 1.4;
  font-weight: 500;
  word-wrap: break-word;
}

.bot-msg {
  background: orange;
  color: white;
  align-self: flex-start;
  animation: text-animation 0.4s ease-in forwards;
  animation-delay: 0.5s;
}

/* @keyframes text-animation {
  from {
    transform: scale(0.9);
  }
  to {
    transform: scale(1);
  }
} */

/* .user-msg {
  background: #e5e5e5;
  color: #333;
  align-self: flex-end;
} */

.user-msg {
  position: relative;
  background: #e5e5e5;
  color: #333;
  align-self: flex-end;
  padding: 8px 12px;
  border-radius: 5px;
  font-size: 14px;
  line-height: 1.4;
  max-width: 80%;
  margin: 4px 0;
}

/* (user side) */
.user-msg::after {
  content: '';
  position: absolute;
  right: -4.5px;
  bottom: 0;
  width: 0;
  height: 0;
  border-top: 8px solid transparent;
  border-bottom: 8px solid transparent;
  border-left: 8px solid #e5e5e5;
}

/* Typing loader */
.typing {
  display: flex;
  gap: 4px;
  align-items: center;
}

.typing .dot {
  width: 8px;
  height: 8px;
  background: orange;
  border-radius: 50%;
  animation: blink 1s infinite;
  opacity: 0.4;
}

.typing .dot:nth-child(2) {
  animation-delay: 0.2s;
}
.typing .dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes blink {
  0%,
  100% {
    opacity: 0.4;
    transform: translateY(0);
  }
  50% {
    opacity: 1;
    transform: translateY(-3px);
  }
}

/* Offer card */
.offer-card {
  background: #fff7e0;
  border: 1px solid orange;
  border-radius: 8px;
  padding: 8px;
  margin-top: 6px;
  font-size: 13px;
  font-weight: bold;
  color: #d86b00;
}

/* Input area */
.input-area {
  display: flex;
  border-top: 1px solid #ddd;
  padding: 8px;
  gap: 5px;
  background: #fffaf0;
}

.input-area input {
  flex: 1;
  padding: 8px;
  border: 1px solid #ccc;
  border-radius: 8px;
  font-size: 14px;
}

.input-area button {
  background: orange;
  color: white;
  border: none;
  border-radius: 8px;
  padding: 8px 12px;
  cursor: pointer;
  font-weight: bold;
}

/* ✅ Responsive for mobile */
@media (max-width: 600px) {
  .chat-container {
    width: 220px;
    height: 330px;
    border-radius: 10px;
  }

  .chat-widget {
    right: 10px;
    bottom: 10px;
  }

  .logo img {
    width: 110px;
    height: 20px;
    margin: 9px 0 9px 0;
  }

  .bot-msg,
  .user-msg {
    font-size: 12px;
  }

  .input-area input {
    font-size: 12px;
    padding: 6px;
  }

  .input-area button {
    padding: 6px 8px;
    font-size: 12px;
  }
}



const chatBox = document.getElementById('chatBox');
const userInput = document.getElementById('userInput');
const sendBtn = document.getElementById('sendBtn');

const offers = {
  internet: [
    '৪৫ জিবি | ৬৯৮ টাকা | ৩০ দিন',
    '৩০ জিবি | ৫৯৮ টাকা | ৩০ দিন',
    '১৫০ জিবি | ৯৯৯ টাকা | ৩০ দিন',
  ],
  minute: [
    '৫০০ মিনিট | ৩৫৭ টাকা | ৩০ দিন',
    '৩০০ মিনিট | ২৫৭ টাকা | ৩০ দিন',
    '৬৫০ মিনিট | ৪১৯ টাকা | ৩০ দিন',
  ],
  sms: [
    '১০০০ SMS | ৯৫ টাকা | ২০ দিন',
    '৫০০ SMS | ৭৫ টাকা | ৩০ দিন',
    '১০০ SMS | ৩৩ টাকা | ৭ দিন',
  ],
};

// Button click & Enter key event
sendBtn.addEventListener('click', handleSend);
userInput.addEventListener('keydown', e => {
  if (e.key === 'Enter') handleSend();
});

function handleSend() {
  const text = userInput.value.trim();
  if (!text) return;
  appendUserMsg(text);
  userInput.value = '';
  const keyword = detectKeyword(text);

  showTyping();

  setTimeout(() => {
    removeTyping();
    if (keyword) showOffers(keyword);
    else
      appendBotMsg(
        'দুঃখিত, আমি মিনিট / ইন্টারনেট / এসএমএস ছাড়া কিছু বুঝতে পারছি না 😅'
      );
  }, 300);
}

function detectKeyword(text) {
  const t = text.toLowerCase();
  if (t.includes('মিনিট') || t.includes('minute') || t.includes('min'))
    return 'minute';
  if (t.includes('ইন্টারনেট') || t.includes('internet') || t.includes('net'))
    return 'internet';
  if (t.includes('sms') || t.includes('এসএমএস')) return 'sms';
  return null;
}

function appendUserMsg(msg) {
  const div = document.createElement('div');
  div.className = 'user-msg';
  div.textContent = msg;
  chatBox.appendChild(div);
  scrollToBottom();
}

function appendBotMsg(msg) {
  const div = document.createElement('div');
  div.className = 'bot-msg';
  div.textContent = msg;
  chatBox.appendChild(div);
  scrollToBottom();
}

function showTyping() {
  const typingDiv = document.createElement('div');
  typingDiv.className = 'bot-msg typing';
  typingDiv.id = 'typing';
  typingDiv.innerHTML =
    '<span class="dot"></span><span class="dot"></span><span class="dot"></span>';
  chatBox.appendChild(typingDiv);
  scrollToBottom();
}

function removeTyping() {
  const typing = document.getElementById('typing');
  if (typing) typing.remove();
}

function showOffers(type) {
  appendBotMsg('আপনার জন্য অফারঃ');
  offers[type].forEach(offer => {
    const card = document.createElement('div');
    card.className = 'offer-card bot-msg';
    card.textContent = offer;
    chatBox.appendChild(card);
  });
  scrollToBottom();
}

function scrollToBottom() {
  chatBox.scrollTop = chatBox.scrollHeight;
}
